<%= form_with(model: tipo_venda, local: true) do |form| %>
<% if tipo_venda.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(tipo_venda.errors.count, "error") %> impediram de salvar:</h2>

  <ul>
    <% tipo_venda.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= form.label :nome %>
  <%= form.text_field :nome %>
</div>

<div class="field">
  <%= form.label :ativo %>
  <%= form.check_box :ativo %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>