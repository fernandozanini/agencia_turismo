<%= form_with(model: tipo_pagamento, local: true) do |form| %>
<% if tipo_pagamento.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(tipo_pagamento.errors.count, "error") %> prohibited this tipo_pagamento from being saved:</h2>

  <ul>
    <% tipo_pagamento.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= form.label :nome %>
  <%= form.text_field :nome, required: true %>
</div>

<div class="field">
  <%= form.label :ativo %>
  <%= form.check_box :ativo %>
</div>

<div class="field">
  <%= form.label :observacao %>
  <%= form.text_field :observacao %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>